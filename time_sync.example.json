{
  "_comment": [
    "Copy to time_sync.json (gitignored — it holds admin passwords).",
    "tolerance_seconds: how far a clock may drift before it counts as wrong.",
    "  The node's `clock` reply only has minute resolution, so a correctly-set",
    "  node can read up to 59s behind. Keep this above 60.",
    "reply_timeout: seconds to wait for an admin CLI reply over the mesh.",
    "Each node needs the advertised name (or a pubkey prefix) and the admin",
    "password. set_time=true lets the script push the correct time; with",
    "false it only reports drift.",
    "Note: firmware refuses to move a clock backwards, so a node running",
    "AHEAD cannot be fixed over the mesh and needs a power cycle.",
    "Metrics (battery, temperature, radio counters) are logged for every node",
    "listed here. Telemetry needs no login, so a node with no password and",
    "set_time=false is graphed without its clock being touched."
  ],
  "tolerance_seconds": 120,
  "reply_timeout": 45,
  "_retries": [
    "A lost packet is normal on LoRa, so an unanswered request is retried.",
    "attempts: total tries per request (login, clock, status, telemetry).",
    "retry_delay: seconds between them. Each retry costs airtime, so keep",
    "  both modest -- 3 attempts across 4 requests is already 12 transmissions",
    "  per node in the worst case.",
    "reset_path_on_retry: before the final login attempt, clear the node's",
    "  stored route so that try floods instead. Helps when a node moved or its",
    "  path went stale, which looks identical to being out of range.",
    "A node that answers with a refusal (wrong password, clock cannot go",
    "backwards) is never retried -- that answer will not change."
  ],
  "attempts": 3,
  "retry_delay": 5,
  "reset_path_on_retry": true,
  "_conflicts": [
    "remove_conflicts_after_days: when a name maps to two keys and the older",
    "  one has been silent this long, ask the node to forget it. A reflashed",
    "  node leaves a dead key behind that keeps shadowing the live one in the",
    "  node's own contact list. Both conditions are required: a conflict, and",
    "  weeks of silence -- a quiet-but-live node is never removed, and an old",
    "  node with no name conflict is never touched. 0 disables removal."
  ],
  "remove_conflicts_after_days": 14,
  "_discovery": [
    "metrics_for_known_nodes: also sample every node the relay has discovered",
    "  (from seen_nodes.json), without listing it here. Telemetry needs no",
    "  login, so this graphs nodes you don't administer. Clocks are never",
    "  touched this way — only nodes listed below can be corrected.",
    "metrics_node_types: which discovered types to sample. Default REP+ROOM;",
    "  adding CLI will sample every handheld that has ever advertised."
  ],
  "metrics_for_known_nodes": false,
  "metrics_node_types": ["REP", "ROOM"],
  "nodes": [
    {
      "name": "Hilltop Repeater",
      "password": "changeme",
      "set_time": true
    },
    {
      "name": "Valley Room Server",
      "password": "changeme",
      "set_time": false
    },
    {
      "pubkey": "a3f9c1",
      "password": "changeme",
      "set_time": true
    },
    {
      "name": "Ridge Repeater (metrics only, no admin password)",
      "set_time": false
    }
  ]
}
